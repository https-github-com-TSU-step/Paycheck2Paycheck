package com.example.paycheck2paycheck.domain.model

enum class RecordingMethod { VOICE, MANUAL }

enum class RecognitionStatus { PENDING, PROCESSING, SUCCESS, FAILED }

enum class StreakStatus { ACTIVE, AT_RISK, BROKEN }